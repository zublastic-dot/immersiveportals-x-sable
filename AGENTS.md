# Zublastic fork instructions

Canonical ecosystem memory is `zublastic-dot/zublastic-context`. Read its
`BOOTSTRAP.md` and this fork's `docs/ZUBLASTIC_CHUNK_TICKETS.md` before material
assumptions or mutations. Start owner-facing replies with `!`.

Follow the ModAutomator CAS lease and schema-v2 work-manifest protocol. Source
code is implementation authority; exact installed hashes and runtime evidence
are deployment authority. Work in isolated branches, preserve upstream credits
and Apache-2.0 licensing, and merge only the validated current PR head.

Keep changes focused. Never hide genuine generation failures to obtain a clean
log. Do not change the owner's mod lineup, world, client or server merely to make
tests pass. Record runtime limitations and mirror material results in canonical
context and `zublastic-ideas`. No billable model calls for owner-side work.
